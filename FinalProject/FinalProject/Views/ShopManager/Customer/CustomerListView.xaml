<Page x:Class="FinalProject.Views.ShopManager.CustomerListView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FinalProject.Views.ShopManager"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CustomerListView">

    <Page.Resources>
        <Style TargetType="TextBlock" >
            <Setter Property="HorizontalAlignment" Value="Right"></Setter>
            <Setter Property="VerticalAlignment" Value="Center"></Setter>
            <Setter Property="Padding" Value="10px"></Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="5px"></Setter>
            <Setter Property="Height" Value="30"></Setter>
            <Setter Property="Width" Value="190"></Setter>
            <Setter Property="TextWrapping" Value="NoWrap"></Setter>
            <Setter Property="HorizontalAlignment" Value="Left"></Setter>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Height" Value="40px"></Setter>
            <Setter Property="Width" Value="100px"></Setter>
        </Style>
    </Page.Resources>
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="8*"></RowDefinition>
            <RowDefinition Height="2*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Grid.Row="0" Content="Add"  Command="{Binding OpenPopupCommand}"></Button>
            <Button Grid.Column="1" Grid.Row="0" Content="Update" Command="{Binding UpdateCommand}"></Button>
            <Button Grid.Column="2" Grid.Row="0" Content="Delete" Command="{Binding DeleteCommand}"></Button>

        </Grid>
        <DataGrid Grid.Column="1" AutoGenerateColumns="False" ItemsSource="{Binding CustomerList}" SelectedItem="{Binding SelectedItem, Mode=OneWayToSource}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Width="0.4*" Binding="{Binding CustomerId}"></DataGridTextColumn>
                <DataGridTextColumn Header="Fullname" Width="1.3*" Binding="{Binding Email}"></DataGridTextColumn>
                <DataGridTextColumn Header="Telephone" Width="*" Binding="{Binding Birthday}"></DataGridTextColumn>
                <DataGridTextColumn Header="EmailAddress" Width="2.2*" Binding="{Binding Password}"></DataGridTextColumn>
                <DataGridTextColumn Header="Birthday" Width="1.2*" Binding="{Binding Birthday, StringFormat=\{0:dd/MM/yyyy\}}"></DataGridTextColumn>
                <DataGridTextColumn Header="Status" Width="0.7*" Binding="{Binding Gender}"></DataGridTextColumn>
                <DataGridTextColumn Header="Password" Width="*" Binding="{Binding CreatedDate, StringFormat=\{0:dd/MM/yyyy\}}"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
